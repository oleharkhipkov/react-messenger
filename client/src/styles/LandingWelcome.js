import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  welcome: {
    fontSize: '26px',
    paddingBottom: '20px',
    color: '#000000',
    fontWeight: '500px',
  },
}));
