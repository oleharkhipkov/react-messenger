import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  welcome: {
    fontSize: 26,
    paddingBottom: 20,
    color: '#000000',
    fontWeight: 500,
  },
}));
