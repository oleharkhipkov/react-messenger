import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  notFound: {
    textAlign: 'center',
  },
  notFoundHeading: {
    fontSize: '32px',
  },
  notFoundText: {
    marginTop: '16px',
  },
}));
