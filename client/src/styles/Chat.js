import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  activeChat: {
    display: 'flex',
    flexDirection: 'column',
    height: '95vh',
    position: 'relative',
  },
}));
